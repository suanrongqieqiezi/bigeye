---
name: "exe打包路径修复流程"
description: "exe打包路径修复流程 — 确保项目打包后在任何电脑上都能直接运行，不依赖开发者本地环境"
tags: [反思, 自动生成]
triggers: ["PyInstaller打包后路径报错", "exe运行时找不到文件"]---

# exe打包路径修复流程

> 确保项目打包后在任何电脑上都能直接运行，不依赖开发者本地环境

## 步骤

- 全局搜索绝对路径模式，如盘符、/Users/、/home/ 等
- 统一替换为相对路径或使用环境变量（如os.path.join、Path）
- 验证数据文件和资源文件的动态获取逻辑
- 打包后在一个全新环境中做冒烟测试，确认无路径报错

## 来源

- 任务反思自动沉淀 2026-08-25 21:13
