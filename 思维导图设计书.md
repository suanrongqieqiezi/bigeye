# 思维导图功能设计书（移动端先行）

> 版本 v1.0 ｜ 2026-08-09 ｜ 由大眼调研代码后编写，茄茄子负责实现
> 范围：移动端优先，桌面端兼容。入口：文件管理顶栏"切换工作区"按钮改造。

---

## 一、功能定位

思维导图是系统的**空间维度**视图，与现有 DAG（时间/执行维度）互补：

- **DAG 流程图**：回答"这个任务怎么推进"——节点是动作（动词），边是执行顺序/数据依赖，有状态流转。
- **思维导图**：回答"我在想什么、各块知识什么关系"——节点是概念/决策/问题（名词），边是自由关联（无方向限制、无状态），允许发散、归类、挂载。

两者关系：**同一话题下的双视图**。DAG 是执行骨架，思维导图是思考空间。互链规则见第六节。

---

## 二、现状盘点（代码调研结论）

### 前端（public/index.html，单文件 6820 行）

| 位置 | 现状 | 说明 |
|---|---|---|
| L926-964 | `#fileBrowser` 文件管理容器 | `fb-header` 顶栏 |
| **L931** | `<button onclick="openPathPicker()">切换工作区</button>` | **本次改造目标** |
| L948 | `#fbWorkspace` 工作区路径条，onclick 也是 `openPathPicker(fbWorkspaceAbs)` | 切换工作区功能**不丢**，此处仍保留 |
| L860-867 | `#flowChartModal` 流程图弹窗 | modal-overlay > modal-box > modal-body > flowChartContent 结构 |
| L813 | 已引入 CDN：marked / dompurify / **mermaid** / **dagre** / **@antv/x6@1.34.1** | X6 全家桶现成，不用新引库 |
| L3991-4045 | `_fcGraph` 实例 + `fcApplyTheme/fcApplyLayout/fcApplyRouter` + `fcReRender` | 主题/布局/连线重渲染体系可参考 |
| L4335 | `buildFlowChartX6(container, x6data, editable)` | X6 图渲染核心，可 clone 出 `buildMindMapX6` |
| L4094 / L5048 | `fetch('/api/topic/'+tid+'/dag')` | 流程图数据获取模式 |
| L811 | `@media(max-width:768px)` 流程图 modal 全屏适配 | 移动端全屏样式已有先例 |

### 后端（server.py，3432 行，自实现 HTTP 服务器）

| 位置 | 现状 | 说明 |
|---|---|---|
| L1777-1814 | `_serve_static` 从 `PUBLIC_DIR`(public/) 服务静态文件 | 新增页面/JS 直接放 public/ 即可被访问 |
| L1820+ | `do_POST` 路由链 `if path == ... elif ...` | 新接口照此追加 |
| L1854-1874 | `POST /api/topic/{tid}/dag` → `DAG.save_x6_json()` | 保存模式参考：只改文本 / 允许结构变更 |
| L879-965 | `task/dag.py get_x6_json()` | 返回 X6 标准 {nodes, edges}，直接喂 `graph.fromJSON()` |
| L966+ | `save_x6_json(x6_data, allow_structural_change)` | 双模式保存，思维导图保存可复用同样思路 |

### DAG 数据格式（get_x6_json 返回）

```json
{
  "nodes": [{"id": "n1", "shape": "dag-node", "task": "描述", "status": "done", "data": {...}}],
  "edges": [{"id": "e1", "source": "n1", "target": "n2", "edgeType": "flow"}]
}
```

---

## 三、入口改造方案

### 3.1 按钮改造（L931）

```html
<!-- 改造前 -->
<button onclick="openPathPicker()">切换工作区</button>
<!-- 改造后 -->
<button onclick="openMindMap()">🧠 思维导图</button>
```

- 切换工作区功能**不丢**：`#fbWorkspace`（L948）仍点击可切换，按钮语义让位给思维导图。
- 可选：按钮加个小图标 + 文字，移动端按钮宽度自适应（fb-header 已有 flex 布局）。

### 3.2 新增弹窗容器（仿 flowChartModal，放在 L867 后）

```html
<div id="mindMapModal" class="modal-overlay" onclick="closeMindMap(event)">
  <div class="modal-box" style="max-width:1320px;width:96vw;height:92vh;max-height:92vh;display:flex;flex-direction:column" onclick="event.stopPropagation()">
    <div class="modal-body" id="mindMapBody" style="flex:1;min-height:0;overflow:hidden;padding:8px;display:flex;flex-direction:column;gap:6px">
      <div id="mindMapToolbar">…（新建节点/折叠全部/布局/缩放按钮）</div>
      <div id="mindMapContent" style="flex:1;min-height:0;width:100%">加载中…</div>
    </div>
  </div>
</div>
```

移动端全屏适配照抄 L811 的 media query 模式（width/height 100vw/vh、圆角 0）。

### 3.3 打开/关闭函数

```js
async function openMindMap(){
  // 显示 modal → fetch GET /api/topic/{tid}/mindmap → buildMindMapX6(container, data, true)
  // 复用 openFlowChart 的骨架（L5041-5129），数据源换成 mindmap 接口
}
function closeMindMap(e){ /* 同 closeFlowChart L5198 */ }
```

---

## 四、数据模型设计

### 4.1 存储位置

独立文件，不动 DAG 表、不污染记忆库：

```
data/missions/{topic_id}/mindmap.json
```

与 `dag.json` 平级，一个话题一张图。无图时接口返回空结构，前端显示空白画布 + "双击创建节点" 引导。

### 4.2 文件结构

```json
{
  "schema": 1,
  "topic_id": "a6ec86b0ff0a",
  "title": "默认：话题标题",
  "updated_at": 1786254000,
  "nodes": [
    {
      "id": "m1",
      "text": "节点标题",
      "note": "详情/备注（可选）",
      "type": "idea",
      "x": 120, "y": 80,
      "color": null,
      "collapsed": false,
      "links": [
        {"kind": "dag", "task_id": "301e61ff103e", "node_id": "85ca507981e6"},
        {"kind": "memory", "fragment_id": 1234},
        {"kind": "file", "path": "data/x.md"}
      ]
    }
  ],
  "edges": [
    {"id": "e1", "source": "m1", "target": "m2", "label": "可选关联说明", "style": "solid"}
  ]
}
```

### 4.3 节点类型（type 枚举）

| type | 含义 | 渲染差异 |
|---|---|---|
| `idea` | 想法/概念（默认） | 常规圆角矩形 |
| `decision` | 决策/结论 | 加粗 + 强调色边框 |
| `question` | 问题/待办 | 虚线边框 + ？角标 |
| `task_ref` | DAG 节点引用 | 左侧色条表示状态（done/run/pending） |

### 4.4 设计要点

- **无层级约束**：edges 自由连线，不强制树形。X6 天然支持任意图。
- **links 与 edges 分离**：edges 是纯视觉关联（用户手画），links 是语义引用（挂到 DAG/记忆/文件），点击可跳转。
- **collapsed**：子节点折叠字段放父节点上（约定：被折叠的父节点把直接子节点们 hide），前端做子树显隐。
- **轻量**：单文件 JSON，不做数据库表。数据量级（百级节点）JSON 完全够，且方便备份/迁移/手工改。

---

## 五、后端 API 设计

在 server.py 的 do_GET / do_POST 路由链中追加（参考 L1854 DAG 接口写法）：

### 5.1 GET /api/topic/{tid}/mindmap

- 返回：`{"ok": true, "mindmap": {...}}`（无文件时返回空结构 `{"nodes":[],"edges":[]}`）
- 实现：读 `data/missions/{tid}/mindmap.json`，不存在则返回空。

### 5.2 POST /api/topic/{tid}/mindmap

- body：`{"mindmap": {...}, "allow_structural_change": true}`
- 返回：`{"ok": true, "mindmap": {保存后最新}}`
- 实现：写回文件 + 更新 updated_at。原子写（先写 .tmp 再 os.replace），防中断损坏。
- 幂等：整图覆盖，前端保存的是全量图，天然幂等，不用做 diff。

### 5.3 可选：POST /api/topic/{tid}/mindmap/import-dag

- body：`{"node_ids": ["85ca507981e6", ...]}`
- 功能：把 DAG 指定节点一键导入为 `task_ref` 节点（保持坐标随机散布一圈），快速建立"执行计划 → 思维空间"的桥。
- 一期可不做，列为二期。

### 5.4 路由注册位置

- do_GET：加在 `elif path == "/api/topic/...dag"` 附近（GET 版 dag 接口在 L1187 之后的链里，找 `startswith("/api/topic/") and endswith("/dag")` 的 GET 分支，对称加 mindmap）
- do_POST：加在 L1854 的 dag POST 分支后面。

---

## 六、与 DAG / 思维链互链

### 6.1 思维导图 → DAG（一期做）

- `task_ref` 节点点击 → 调 `openFlowChart()` 打开流程图，并高亮对应节点（X6 `graph.getCellById(node_id).addTools(...)` 或滚动到节点位置）。
- 导入：toolbar 放"导入任务节点"按钮，调 5.3 接口（或前端直接拿现有 DAG x6 数据生成 task_ref 节点，不走后端也行，前端已有 `/api/topic/{tid}/dag` 数据）。

### 6.2 DAG → 思维导图（二期）

- DAG 节点详情里加"在思维导图中查看"链接，定位到对应 task_ref 节点。
- 需要 task/dag.py 的 node 里加可选 `mindmap_id` 字段，或反向查 mindmap.json。

### 6.3 思维链（二期）

- 思维导图节点可挂 `kind: "thought", chain_step_id` 引用思维链某步，点击跳转对应话题的思维链查看。

---

## 七、移动端交互设计（重点）

### 7.1 画布操作（X6 自带能力，需开启）

```js
panning: { enabled: true, eventTypes: ['leftMouseDown', 'touchStart', 'mouseWheel'] },
mousewheel: { enabled: true, modifiers: [], minScale: 0.2, maxScale: 2.5 },
embedding: false,
connecting: { snap: true, allowBlank: false, allowLoop: false }
```

- **单指拖动**：平移画布（X6 panning 原生支持 touch）。
- **双指捏合**：缩放（X6 mousewheel 不支持 touch pinch，需自己监听 `touchmove` 双指距离比 → `graph.zoom()`，参考 flowChart 已有 `initFlowChartZoom` L5141 附近可借鉴思路，或简单方案：toolbar 加减号按钮 + 双击节点放大）。
- **双击空白**：创建 `idea` 节点（buildFlowChartX6 里 blank:dblclick 已统一处理，照抄）。
- **长按节点**：弹出操作菜单（编辑文本 / 改类型 / 添加子节点 / 挂链接 / 删除 / 折叠展开）。
- **拖动节点**：自由移动位置，松手自动保存（debounce 2s 或手动保存按钮）。

### 7.2 节点编辑

- 单击节点 → 选中（高亮）→ 顶部 toolbar 出现编辑按钮，或直接复用 fcNodeEditor 弹窗（L4127 附近已有"取消/确定"编辑器 modal，clone 一份改字段）。
- 编辑内容：text（必填）、note（可选）、type（下拉）、color（色板）、links（挂 DAG 节点/记忆/文件）。

### 7.3 连线

- 节点边缘 magnet（buildFlowChartX6 L4447 已有端口配置）拖出连线，松手吸附目标节点。
- 双击连线 → 编辑 label 或删除。
- 连线样式：solid 默认；`decision` 之间可用 dashed 表示"备选路径"。

### 7.4 布局辅助（toolbar 按钮）

- 🌲 自动布局（dagre，已有 dagre CDN，照 L4338-4340 的 layout 配置）
- 📖 折叠/展开全部
- 🔍 适应画布（graph.zoomToFit）
- 💾 保存（显式按钮，同时自动保存兜底）

### 7.5 移动端细节

- 全屏 modal，顶部 toolbar 固定，画布占满剩余空间。
- 节点字号 ≥ 12px，点击目标 ≥ 40px 触控区（X6 node 尺寸调大）。
- 输入用系统键盘，editor 弹窗底部对齐避免被键盘遮挡（`visualViewport` 适配）。
- 保存时机：每次结构变更（加/删/改/连线）后 debounce 自动保存 + 关闭 modal 时强制保存。

---

## 八、实施步骤（按依赖排序）

1. **后端数据层**：写 `mindmap.json` 读写模块（可放 server.py 内函数，或独立 `mindmap.py`），实现 GET/POST 接口，curl 验证。
2. **前端骨架**：加 modal 容器 + `openMindMap/closeMindMap` + GET 拉取数据 + 空画布显示。
3. **前端渲染**：clone `buildFlowChartX6` → `buildMindMapX6`（简化样式：无状态色条、支持 type 样式、自由拖拽），能画出节点和边。
4. **交互**：双击建节点、长按菜单、节点编辑器、拖拽连线、折叠/自动布局/缩放按钮。
5. **保存链路**：debounce 自动保存 + 关闭强制保存，刷新后数据还在。
6. **入口改造**：L931 按钮改"思维导图"，验证切换工作区仍可通过 fbWorkspace 使用。
7. **互链**：task_ref 点击跳转流程图高亮；导入 DAG 节点按钮。
8. **打磨**：移动端手势（双指缩放）、触控尺寸、键盘适配、全屏样式。

## 九、验收标准

- [ ] 移动端从文件管理顶栏进入思维导图，全屏展示无遮挡
- [ ] 双击空白建节点、双击节点改文本、长按弹菜单、拖拽连线全部可用
- [ ] 单指拖画布、双指缩放流畅（或 toolbar 缩放可用）
- [ ] 修改后刷新页面，图完整恢复（含位置、连线、类型、折叠状态）
- [ ] 切换工作区功能仍可通过 fbWorkspace 路径条使用
- [ ] task_ref 节点点击能打开流程图并高亮对应节点
- [ ] 桌面端打开不破版（复用 flowChart 的响应式）

---

## 十、风险与取舍

| 风险 | 应对 |
|---|---|
| 双指缩放 X6 不原生支持 | 一期 toolbar 缩放按钮兜底；二期手写 touch pinch → graph.zoom() |
| 单文件 index.html 膨胀 | 思维导图 JS 可放独立 `public/mindmap.js`，`_serve_static` 已支持 .js，index.html 加一个 `<script src="/mindmap.js">` |
| 保存并发覆盖 | 单用户场景低风险；写文件用 tmp+replace 原子化 |
| 与 DAG 数据漂移 | task_ref 只存 id 不存文本，展示时实时查 DAG（或前端已有缓存），避免双份文本过期 |
| 一期范围过大 | 严格按第八节顺序，1-5 是 MVP（能建能存能交互），6-8 逐步加 |
