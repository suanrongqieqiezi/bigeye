# 晶体记忆网络设计书

> CMN — Crystal Memory Network
> AI 的元认知与永久记忆系统设计书
> 版本：v1.0 · 唯一权威设计文档

---

## 第一章：问题与目标

### 1.1 LLM 的根本困境

大型语言模型本质是无状态函数 `f(prompt) → output`，每次调用从零开始。由此衍生四个根本困境：

| 困境 | 表现 | 现有补丁方案 |
|------|------|-------------|
| 窗口有限 | 上下文塞不下大文件/长历史 | 加长上下文（治标） |
| 跨会话失忆 | 换任务即遗忘 | RAG 检索（治标） |
| 无元认知 | 不知道自己知道什么、不知道什么 | system prompt 硬编码人格（治标） |
| 信息平权 | 所有内容同等重要，抓不住重点 | 摘要压缩（治标） |

现有方案都是补丁式应对，各自解一个面，没有从根因上统一解决。

### 1.2 CMN 的目标

给 AI 一套"元认知操作系统"——不改模型参数，而是设计一套行为规则与存储规则，从根因上同时解这四个困境，让 AI：

1. **看得见比自己大几个数量级的数据**（分层金字塔）
2. **跨会话持续积累**（晶体持久化）
3. **知道自己的认知状态**（元认知与自检）
4. **只在高密度信息上花 token**（晶体泥沙分离）

---

## 第二章：核心设计哲学

### 2.1 内容寻址，而非位置寻址

传统文件系统是位置寻址（`/path/to/file`），内容变了路径不变，AI 无法判断"我上次看的东西还是不是这个东西"。

CMN 用 hash 作为节点的唯一身份证：
- **内容没变 → hash 不变** → "我见过的"判断零成本成立，晶体直接复用
- **内容变了 → hash 变了** → "旧结论可能过期"自动可感知

**收益**：AI 从"每次都从零读"变成"只读变化的部分"——质变。

### 2.2 晶体与泥沙分离

不是所有信息生而平等。绝大多数是泥沙（过程、尝试、流水账），只有极少是晶体（结论、因果、决策）。

传统 RAG 把所有 chunk 平等检索，token 全花在泥沙上。CMN 强制压缩时回答三问：

1. **结论是什么？**
2. **为什么做这个决策？**
3. **下一步做什么？**

答不出 → 压缩没到位，继续提炼。

| | 晶体（Crystal） | 泥沙（Sediment） |
|---|----------------|-----------------|
| 定义 | 有因果关系的结论/决策/判断 | 流水账、中间尝试、推理过程 |
| 价值 | 可复用 | 仅用于溯源 |
| 存储 | 保留详细语义 | 1~2 句跳过或不存 |

**收益**：同样 token 预算下，AI 拿到的信息密度高一个数量级。

### 2.3 分层金字塔，对抗无限增长

信息无限增长，上下文窗口有限。解法只有一种：递归压缩。

```
原始数据（无限）→ 切片摘要 → 层N（仍可能很大）
                                ↓
                          再切片摘要 → 层N-1
                                ↓
                              ...
                                ↓
                          顶层 → 塞入上下文
```

每一层是下一层的有损压缩，但保留"入口"——模型可从顶层任意下钻到某一层某一片的原始内容。

**收益**：AI 第一次能"看见"比自己上下文大几个数量级的数据库，是结构化全局视野而非局部采样。

### 2.4 自传式成长

AI 不只是被动存数据，而是主动反思"发生了什么/为什么/下次怎么办"，把反思结果分层存放：
- **行为层**：应该/不应该做什么 → 约束未来行为
- **工作流层**：技术方法/流程 → 复用流程
- **经验层**：具体教训 → 语义召回

**收益**：AI 从"无状态函数"变成"有成长曲线的实体"。第二次遇到相似问题，能召回"上次这么做失败了，因为 X"。

---

## 第三章：两大子系统

CMN 是顶层概念，下面挂两个子系统，共享 hash 寻址空间、向量检索、因果追溯，但节点类型严格隔离。

### 3.1 自传晶体（Autobiographical Crystal）

**定位**：AI 的自传人格 + 纠错知识库。

**来源**：AI 内部反思、判断、教训。

**可变性**：可覆写（认知更新时旧晶体被否定边标记，新晶体产生）。

**粒度**：任意（一句话、一段反思、一个决策）。

**价值**：巩固纠正 AI 行为、防止再次犯错、形成自传人格完善"过去"记忆。

### 3.2 文件晶体（File Crystal）

**定位**：对外部真实文件的建库索引与分层摘要。

**来源**：外部真实文件（代码、文档、数据库导出等）。

**可变性**：hash 不变则晶体永久有效；hash 变了 → 该切片晶体标过期 → 按需重读重摘要。

**粒度**：固定切片（便于金字塔堆叠）。

**价值**：AI 不需要重复阅读已看过的内容；可快速检索、定位、修改文档与代码。

### 3.3 为什么要统一

两者本质相同——内容寻址 + hash 一致性 + 分层摘要 + 晶体复用。差异只在来源和可变性。统一后：
- 共享同一套 hash 寻址空间
- 共享同一套向量检索
- 共享同一套因果追溯
- AI 可在一个网络里同时走"我的判断"和"文件事实"两条线

### 3.4 认识论隔离（统一的前提）

统一是必要的，但**必须在节点类型上严格区分**，否则 AI 召回时会把"自己的猜测"当"文件事实"用，引发认识论混乱。

每个晶体节点必须带认识论标记：
- **world**：来自外部文件的事实（hash 不变则永久有效）
- **experience**：AI 的经验教训（可覆写）
- **opinion**：AI 的判断/观点（可被否定边推翻）

节点类型 + 边类型双重隔离，AI 召回时明确知道"这是什么性质的知识"。

### 3.5 晶体等级：普通 vs 权威

不是所有晶体可信度相同。AI 可能拿一次性经验当铁律用，也可能拿网络抓的垃圾信息当事实。权威库填这个缺口。

| | 普通晶体 | 权威晶体 |
|---|---------|---------|
| 来源 | 任意 | 教科书/官方文档/多源印证/多次验证 |
| 原数据留存 | raw_source 可选 | **强制留底**（图书馆属性） |
| 覆写门槛 | 新晶体 + 否定边即可 | 需新权威晶体 + 强证据 |
| 用途 | 日常参考 | 铁律、教科书式知识 |
| 比喻 | 笔记本 | 图书馆/记忆宫殿 |

**权威性纯内部机制判定，不靠用户指定**。AI 不应依赖用户提醒"这是权威"——那等于把元认知责任推给人。

判定信号三条，满足任一即由反思回路自动提拔：
1. **来源类型**：教科书/官方文档/权威期刊（来源元数据自动识别）
2. **多源印证**：≥3 个独立来源说同样的事
3. **多次验证**：同一条经验被反思回路反复确认没被推翻

AI 自己单方面判断的，只进普通晶体，不能进权威库。

**权威可成长也可被推翻**：
- 升权威：反思回路发现普通晶体满足判定条件，自动提拔，加 authority 标记
- 推翻权威：必须有新权威晶体 + 否定边 + 理由。普通晶体不能直接否定权威晶体

### 3.6 物理存储模型

晶体挂载点分"位置"和"身份"两层：**挂载点是位置（可变），hash 是身份（不变）**。位置变不影响身份（文件移动了路径，hash 不变晶体还在）；身份变影响有效性（hash 变了晶体过期）。

| 晶体类型 | 挂载点 | 寻址方式 |
|---------|--------|---------|
| 自传晶体-行为层/工作流层 | 全局命名空间 | hash + 全局检索 |
| 自传晶体-经验层 | 先挂任务，反思后可提为全局 | 任务内优先，提为全局后跨任务 |
| 文件晶体 | 挂文件路径 | 路径 → hash → 晶体 |
| 网络晶体 | 挂 URL | URL → hash → 晶体 |

**两类物理文件夹严格隔离**：

| 文件夹 | 内容 | 来源 | 写入权限 | 认识论标记 |
|--------|------|------|---------|-----------|
| `权威库/`（如 `knowledge_base/`） | 教科书、官方文档、权威资料 | 用户手动放进去 | AI 只读，不能写 | world + authority |
| `下载库/`（如 `ai_downloads/`） | AI 网上搜到下载的内容 | AI 主动抓取 | AI 读写 | world（可信度看来源） |

两类都建文件晶体（切片→hash→金字塔），但权限和可信度明确隔离：
- 权威库稳定不动，AI 拿到的就是铁律
- 下载库随搜随建，URL 变了按需重抓
- 两者共享同一套 hash 寻址 + 向量检索
- AI 拿"下载库的事实"去推翻"权威库的结论"——不行，权限不够

**原数据留底分级**：
- 权威库文件：留路径 + hash + 切片坐标（不重复存原文，原文在物理文件里）
- 下载库内容：**强制全量留底**（URL 内容可能变/失效，不留就找不回）
- 经验原数据：留触发这次经验的任务上下文片段（不是整个任务日志）

---

## 第四章：晶体网络

之所以叫"网络"，是因为整理总结晶体时，顺带把可能关联的、甚至关联起来能想起更好的总结，形成新的晶体。这些关系构成网络，让 AI 可以：
- 一步步提炼成晶体
- 迅速回溯到"当初为什么这么想"
- 自检"还有没有漏洞"（找原始记忆文件验证）

无论如何都比现有方案"全部文件重新读一遍"省 token。

### 4.1 四大网络机制

| 机制 | 作用 | 价值 |
|------|------|------|
| **追溯** | 回到"当初为什么这么想" | 找根因，不堆补丁 |
| **关联** | 总结时顺带想起相关的 | 触发新洞察 |
| **涌现** | 关联起来产生新晶体 | 知识自我生长 |
| **自检** | 发现"还有没有漏洞" | 找缺口补全 |

前三个是"向后看"（理解过去），最后一个是"向前看"（发现不足）。

### 4.2 五种关系边

"关联"是个筐，什么都往里装就废了。LLM 天然过度联想，必须分类约束。至少五种边，强弱不同：

| 边类型 | 语义 | 强度 | 用途 |
|--------|------|------|------|
| **因果** | 因为 A 所以 B | 最强 | 追根因 |
| **派生** | C 由 A+B 融合而成 | 强 | 血统溯源 |
| **支持** | 证据 A 支撑结论 B | 强 | 证据链 |
| **否定** | 新晶体推翻旧晶体 | 中（带时间戳） | 防止用过期结论 |
| **关联** | A 和 B 相关 | 弱（带 confidence + 可解释理由） | 检索加分 |

弱关联必须带 confidence + 可解释（标注"为什么关联"），否则网络会被 LLM 的过度联想灌爆。

### 4.3 双链路追溯

晶体是压缩态，每压一层丢一次信息。如果回溯只能拿到上一层晶体，那找漏洞永远找不到——漏洞往往在被压缩丢弃的细节里。

每个晶体节点必须同时持有两条指针：
- `crystal_parent` → 它由哪个晶体派生/压缩而来（思维血统）
- `raw_source` → 它的原始素材在哪（文件切片 id / 记忆碎片 id / 工具输出原文）

自检时先沿 `crystal_parent` 看逻辑链，发现疑点再沿 `raw_source` 下钻到原始层验证。少了一环，"找漏洞"就是空话。

### 4.4 节点的认识论标记

节点除类型隔离外，还必须维护认识论一致性。AI 召回时能区分：
- 这是文件事实（hash 不变则永久有效）
- 这是我的判断（可覆写，可能错）
- 这是已被推翻的旧判断（否定边标记）

节点类型 + 边类型双重隔离，防止 AI 把旧猜测当事实用。

---

## 第五章：三大决策

经讨论确定的三个核心决策，决定整个系统的行为模式。

### 5.1 按需验证，不主动传播失效

**决策**：文件变化时不主动沿派生边向上传播失效标记，AI 主任务用晶体时不主动查 hash。

**理由**：主任务不拖慢、维护成本低。AI 没被要求做的不管那么多。

**连带约束 — 信任但可验证**：按需验证意味着 AI 可能拿到过期结论。底线是 AI 要能**知道自己可能在用过期东西**，否则比没记忆还危险。

解法不是主动查（那就成自动传播了），而是给每个晶体标"置信度衰减"信号：
- 文件晶体：hash 最后一次验证的时间戳。越久没验证，AI 越应"留个心眼"
- 自传晶体：被多少新晶体引用/否定过。老没人引用的，可能已被推翻但没显式否定

AI 用晶体时看一眼标记，自己决定要不要下钻验证。**不强制，但有信号**。

### 5.2 异步混合的网络增长

**决策**：强关系（因果/派生/支持/否定）同步建，弱关联异步批量建。

**理由**：
- 强关系是事实，必须当时记下，否则事后想不起来
- 弱关联是推测，反思时再判定质量更高，且不拖慢主任务

**增长节奏**：
- 主任务执行时：只记晶体本体 + 强关系边
- 空闲反思时：扫描孤立/新晶体，批量判定要不要建弱关联边、要不要涌现元晶体

### 5.3 反思回路的四项职责

异步混合把所有"网络生长"的活压到反思回路上。反思回路要是拉胯，网络永远长不出好关联。必须干四件事，缺一不可：

| 职责 | 做什么 | 不做的后果 |
|------|--------|-----------|
| **建弱关联** | 扫描孤立/新晶体，判定要不要建关联边 | 网络稀疏，检索命中率低 |
| **涌现元晶体** | 发现晶体群共通模式，提炼元晶体 | 知识不升维，停留在具体经验 |
| **反熵修剪** | 删失效边、合并重复晶体、降权弱关联 | 网络熵增成垃圾场 |
| **自检缺口** | 发现"这里我还没有晶体"的盲区 | AI 不知道自己不知道 |
| **提拔权威** | 扫描普通晶体，满足来源类型/多源印证/多次验证者提拔进权威库 | 权威库不成长，铁律稀少 |

**反熵修剪最容易被忽略但最关键**。只长不剪的网络必成屎山。

**提拔权威的纪律**：反思回路判定满足条件才提拔，AI 单方面判断的只能进普通晶体。权威晶体一旦提拔，覆写门槛抬高——必须有新权威晶体 + 否定边 + 理由才能推翻。

---

## 第六章：检索与使用

### 6.1 双路检索

异步增长意味着新晶体刚建立时没有关联边，要等下次反思才补。这期间只能靠 hash 寻址。所以检索必须有两条路：

- **hash 寻址**：知道 id/hash 直接拿——任何时候都行，给孤立节点兜底
- **网络走边**：沿关系边跳——只有反思建过边的节点能用

两条路必须都存在，且 hash 寻址是兜底。否则新晶体在反思前是"隐形"的，记了等于没记。

### 6.2 下钻路径

金字塔顶层塞入上下文后，AI 可按需下钻：
- 从顶层摘要发现疑点 → 沿 `crystal_parent` 找到派生它的下层晶体
- 下层晶体还不够 → 沿 `raw_source` 找到原始切片
- 原始切片 hash 已变 → 标记过期，触发按需重读重摘要

下钻不是随机的，而是沿双链路有目的地走。

### 6.3 权威库的使用纪律

- AI 召回权威晶体时，可直接当铁律用（信任度高，置信度衰减慢）
- AI 召回普通晶体时，应看一眼"置信度衰减"信号，自己决定要不要下钻验证
- AI 不能用普通晶体否定权威晶体，必须先有新权威晶体 + 否定边
- AI 不能写入 `权威库/` 物理文件夹，只能读
- AI 写入 `下载库/` 后，可对其内容建文件晶体，但认识论标记为 world（非 authority）

---

## 第七章：能力收益对照

| 能力 | 无 CMN | 有 CMN |
|------|--------|--------|
| 看大数据库 | 采样局部，瞎子摸象 | 金字塔顶层全览，按需下钻 |
| 跨会话积累 | 失忆，重头来过 | 晶体持久化，越用越聪明 |
| 自我纠错 | 同样的错反复犯 | 召回"上次失败教训" |
| token 效率 | 全量塞入或随机检索 | 晶体复用 + 只读变化部分 |
| 知识一致性 | 不知道自己记的是否过期 | hash 信号 + 按需验证 |
| 追根因 | 无法回溯 | 双链路下钻到原始素材 |
| 找漏洞 | 不知道哪里不懂 | 自检盲区 + 下钻验证 |

---

## 第八章：风险与边界

### 8.1 晶体质量决定一切

如果 LLM 摘要质量差（废话、丢关键信息），金字塔越往上越垃圾，AI 拿到的是"压缩到失真的废料"。这比不做还糟。

**摘要提示词工程是成败关键**，不是次要问题。三问压缩必须严格执行，答不出不收。

### 8.2 边的纪律是网络的命脉

LLM 天然倾向于"什么都关联"。如果不强制定义"什么时候允许建边、建什么类型的边、什么时候必须删边"，网络会以惊人速度熵增成垃圾场。

五种边类型 + 反思回路的反熵修剪，是约束网络质量的硬纪律。

### 8.3 沉没成本权衡

现有记忆系统已有基础（FragmentStore/EntityStore/SummaryTree）。推进 CMN 时需权衡：
- 在现有基础上加文件晶体 + 网络化改造 → 风险低，但可能受历史包袱
- 整个记忆层推翻重写 → 干净，但沉没成本大

**原则**：不为统一而统一。现有能用的逻辑保留，新增的按 CMN 哲学设计，逐步融合。

### 8.4 不做什么

- 不做主动失效传播（按需验证，见 5.1）
- 不做同步涌现（异步混合，见 5.2）
- 不做无类型区分的"大杂烩"关联（认识论隔离，见 3.4）
- 不做只长不剪的网络（反熵修剪，见 5.3）

---

## 附录：术语表

| 术语 | 定义 |
|------|------|
| **CMN** | Crystal Memory Network，晶体记忆网络 |
| **晶体** | 有因果关系的结论/决策/判断，可复用 |
| **泥沙** | 流水账、中间尝试、推理过程，仅用于溯源 |
| **自传晶体** | AI 内部反思产出的晶体，可覆写 |
| **文件晶体** | 外部文件切片摘要产出的晶体，hash 不变则永久有效 |
| **金字塔** | 文件晶体的递归分层摘要结构 |
| **双链路** | crystal_parent（思维血统）+ raw_source（原始素材） |
| **五种边** | 因果/派生/支持/否定/关联 |
| **三问压缩** | 结论是什么/为什么/下一步做什么 |
| **三问反思** | 发生了什么/为什么/下次怎么办 |
| **认识论标记** | world/experience/opinion 三态，区分知识性质 |
| **权威晶体** | 满足来源类型/多源印证/多次验证的晶体，覆写门槛高 |
| **authority 等级** | 普通vs权威的区分标记，决定覆写门槛和召回信任度 |
| **权威库** | 物理文件夹，存教科书/官方文档等权威资料，AI 只读 |
| **下载库** | 物理文件夹，存 AI 网络抓取的内容，AI 读写 |
| **反熵修剪** | 反思回路中删失效边、合并重复、降权弱关联的职责 |
| **按需验证** | 不主动传播失效，AI 用晶体时自己决定是否下钻验证 |
| **异步混合** | 强关系同步建，弱关联反思批建 |
| **原数据留底** | 权威晶体强制留存原始素材，普通晶体可选 |

---

> 本设计书只讲设计哲学与架构，不涉及代码实现细节。
> 实现计划待设计书定稿后单独编写。
> 版本：v1.0
